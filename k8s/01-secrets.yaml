apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: escola-system
  labels:
    app.kubernetes.io/part-of: sistema-escolar
type: Opaque
# NOTA: Em producao, usar Sealed Secrets ou External Secrets Operator
# Os valores abaixo estao em base64 (echo -n 'valor' | base64)
data:
  POSTGRES_USER: cG9zdGdyZXM=          # postgres
  POSTGRES_PASSWORD: cG9zdGdyZXMxMjM=  # postgres123
