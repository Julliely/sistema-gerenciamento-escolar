##############################################
# Deployment - Matricula Service
##############################################
apiVersion: apps/v1
kind: Deployment
metadata:
  name: matricula-service
  namespace: escola-system
  labels:
    app: matricula-service
    app.kubernetes.io/part-of: sistema-escolar
spec:
  replicas: 2
  selector:
    matchLabels:
      app: matricula-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: matricula-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8082"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: matricula-service
          image: escola/matricula-service:latest
          ports:
            - containerPort: 8082
          env:
            - name: POSTGRES_HOST
              valueFrom:
                configMapKeyRef:
                  name: escola-config
                  key: POSTGRES_HOST
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: POSTGRES_PASSWORD
            - name: CADASTRO_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: escola-config
                  key: CADASTRO_SERVICE_URL
            - name: DISCIPLINA_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: escola-config
                  key: DISCIPLINA_SERVICE_URL
          resources:
            requests:
              cpu: 250m
              memory: 384Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 20

---

##############################################
# Service - Matricula Service (ClusterIP)
##############################################
apiVersion: v1
kind: Service
metadata:
  name: matricula-svc
  namespace: escola-system
  labels:
    app: matricula-service
    app.kubernetes.io/part-of: sistema-escolar
spec:
  type: ClusterIP
  selector:
    app: matricula-service
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
